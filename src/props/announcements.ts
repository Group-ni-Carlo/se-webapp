export interface AnnouncementDataProps {
  id: number;
  title: string;
  caption: string;
  imageSrc: string;
}

export interface MerchDataProps {
  id: number;
  title: string;
  caption: string;
  price: number;
  imageSrc: string;
}
